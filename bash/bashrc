#Get python version

# If not running interactively, don't do anything
[ -z "$PS1" ] && return

# Set the default editor
export PATH=$HOME/.local/bin:$HOME/usr/script:/usr/clang_3_4/bin:$HOME/usr/phantomjs/bin:$HOME/usr/venv/bin:$PATH

if [ -d "$HOME/bash_conf" ]; then

    CUSTOM="$HOME/bash_conf/*"
    for conf in $CUSTOM
    do
        source "$conf"
    done
fi

export CLASSPATH="$HOME/.local/bin/js.jar"
export EDITOR=vim

#Some path for daily use
export STARDICT_DATA_DIR=$HOME/stardict/dic/
export UVA_PATH=$HOME/github/codeSet/c/Uva/

#Get color support for 'less'
export LESS="--RAW-CONTROL-CHARS"
export NVM_DIR="/home/jordan/.nvm"

[ -e /etc/default/locale ] &&  source /etc/default/locale

#Enable programmable completion features.
if [ -f /etc/bash_completion ]; then
   source /etc/bash_completion
fi

#Enable Powerline bash prompt
if [ -f /usr/lib/python2.7/site-packages/powerline/bindings/bash/powerline.sh ]; then
   source  /usr/lib/python2.7/site-packages/powerline/bindings/bash/powerline.sh 
fi
if [ -f ~/.local/lib/python2.7/site-packages/powerline/bindings/bash/powerline.sh ]; then
   source ~/.local/lib/python2.7/site-packages/powerline/bindings/bash/powerline.sh 
fi

# Enable autojump function
[[ -s ~/.autojump/etc/profile.d/autojump.sh ]] && source ~/.autojump/etc/profile.d/autojump.sh 

# Enable bashmarks
[ -e $HOME/.local/bin/bashmarks.sh ] && source $HOME/.local/bin/bashmarks.sh
# Set the PS1 prompt (with color).
#Enable Powerline
PS1='${debian_chroot:+($debian_chroot)}\[\033[01;32m\]\u@\h\[\033[00m\]:\[\033[01;34m\][ \W ]\[\033[00m\]\$ '


# don't put duplicate lines or lines starting with space in the history.
# See bash(1) for more options
HISTCONTROL=ignoreboth
HISTSIZE=1000
HISTFILESIZE=2000
# append to the history file, don't overwrite it
shopt -s histappend

# check the window size after each command and, if necessary,
# update the values of LINES and COLUMNS.
shopt -s checkwinsize
shopt -s histverify


#Use vim as man page viewer
#export MANPAGER="/bin/sh -c \"unset PAGER;col -b -x |  vim -R -c 'set ft=man nomod nolist' -c 'map q :q<CR>' -c 'map <SPACE> <C-D>' -c 'map b <C-U>' -c 'nmap K :Man <C-R>=expand(\\\"<cword>\\\")<CR><CR>' -\""


# Use colors for less, man, etc.
[[ -f ~/.LESS_TERMCAP ]] && . ~/.LESS_TERMCAP


[ -s "$NVM_DIR/nvm.sh" ] && . "$NVM_DIR/nvm.sh"  # This loads nvm
